<?xml version="1.0" encoding="UTF-8"?>
<Package Name="$(PackageName)" xmlns="http://opentap.io/schemas/package" InfoLink="http://www.keysight.com/find/TAP" Version="$(GitVersion)" OS="$(OS)" Architecture="$(Architecture)">
  <Variables>
    <Platform>Windows</Platform>
    <Architecture>x86</Architecture>
    <Configuration>The platform $(Platform)-$(Architecture) is cool!</Configuration>
    <Owner>Keysight Technologies</Owner>
    <PackageName>OpenTAP</PackageName>
    <SourceUrl>https://gitlab.com/OpenTAP/opentap</SourceUrl>
  </Variables>
  <Description>This is the $(PackageName) package that contain the most common functionality used in test automation. It controls test plan (list of test steps) execution and provides access to functionality offered by plugins. This covers logging functionality, result handling (database) and instrument communication (user-developed hardware drivers, for example). OpenTAP also includes a CLI and a plugin manager that finds and handles plugins (test steps, DUT and Instrument interfaces, etc.).
    $(Configuration)
    <Status>Released</Status>
    <Organisation>$(Owner)</Organisation>
  </Description>
  <SourceUrl>$(SourceUrl)</SourceUrl>
  <Owner>$(Owner)</Owner>
  <Files>
    <File Path="Sqlite.$(OS).dll"/>
  </Files>
  <PackageActionExtensions>
    <ActionStep ExeFile="tap.exe" ActionName="install" Condition="'$(OS)'  ==   'Windows'"/>
  </PackageActionExtensions>
</Package>
